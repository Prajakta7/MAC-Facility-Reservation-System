<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pshashank.facilitiesmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ViewSelectedViolationActivity"
            android:label="@string/title_activity_view_selected_violation"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".ViewViolationsActivity" />
        <activity android:name=".ModifyReservationActivity" />
        <activity android:name=".BookReservationActivity" />
        <activity android:name=".RequestReservationActivity" />
        <activity android:name=".ViewReservationsActivity" />
        <activity android:name=".UpdateProfileActivity" />
        <activity
            android:name=".ScrollingActivity1"
            android:label="@string/title_activity_scrolling1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserProfileActivity"
            android:label="@string/title_activity_user_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".FMActivity" />
        <activity android:name=".AdminHomeScreen" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserHomeScreen" />
    </application>

</manifest>